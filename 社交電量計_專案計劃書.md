# 社交電量計（Social Battery Meter）專案計劃書

**FUTUREMODE 2026 · BUILDMODE Hackathon**
**賽道：AI for Everyday Life 日常生活 AI**
**開發期程：3 天（Sep 4–6, 2026）**

---

## 1. 專案概念

### 1.1 一句話介紹
用 AI 幫「社交能量」建模——像手機電池一樣，預測每個社交活動會消耗多少心理能量，並幫使用者規劃一週行程，避免過度消耗導致 burnout。

### 1.2 問題陳述
- 內向者或高敏感族群常常沒有意識到自己的社交能量已經見底，直到情緒崩潰或極度疲憊才驚覺。
- 現有的行事曆 App（Google Calendar 等）只管理時間，不管理「心理資源」。
- 心理學上（Big Five 的 Extraversion 維度、Social Battery 概念）已有理論基礎，但缺乏把它產品化、視覺化、可規劃的工具。

### 1.3 解法核心
1. **社交人格快篩**：5–8 題測驗，決定使用者的「基礎電池容量」與「充電方式」。
2. **電量消耗預測引擎**：使用者輸入/選擇社交活動（類型、人數、熟悉度、時長），AI 根據心理學規則估算消耗百分比。
3. **一週行程總覽 + 風險預警**：視覺化電池狀態，AI 標示「電量過低」的日子並給建議。
4. **事後回報校準**：活動結束後回報實際消耗，讓系統對這個使用者的預測越來越準（demo 時展現「學習」效果）。

### 1.4 創新點
- 把抽象的心理學概念（社交能量、外向性光譜）轉化成直覺、可視覺化、可規劃的產品介面。
- 不是通用的心情日記 App，而是**未來導向的資源規劃工具**——像理財 App 管理金錢一樣管理社交能量。
- 用 LLM 做「個人化消耗係數推論」，而非寫死規則，讓系統能對不同人格类型給出不同建議。

---

## 2. 最小可行產品（MVP）範圍

### 必做（3 天內一定要完成）
1. 社交人格快篩問卷 → 產生基礎電池容量 + 人格摘要
2. 新增/編輯社交活動（類型、人數、熟悉度、時長、日期時間）
3. AI 電量消耗計算（呼叫 OpenAI API，用 prompt engineering，不用自建模型）
4. 電池視覺化 UI（單日 + 一週總覽）
5. 風險預警文字建議（AI 生成，例如：「週三有兩場社交活動，建議中間安排 30 分鐘獨處」）
6. 事後回報校準（簡單的「比預期多/少/差不多」三選一 + 下次調整權重）

### 加分項（時間允許再做）
- 語音輸入活動描述（可接 ElevenLabs API，剛好是贊助商之一，現場可能有加碼獎勵）
- 分享電池狀態卡片到社群（圖片產生）
- 深色模式 / 更精緻的電池動畫（SVG 液體效果）

### 明確不做（避免範圍膨脹）
- 不做手機原生 App（先做 Web App，Responsive 設計，之後可包裝成 PWA 或用 Capacitor 轉 App）
- 不做真實使用者帳號系統的複雜權限管理（先用簡單的 email/匿名 session 即可）
- 不接手機使用行為的被動感測（螢幕時間、GPS 等）——這需要原生 App 權限，3 天做不完
- 不做多人協作/社交網路功能

---

## 3. 技術架構

### 3.1 前端
- **框架**：Next.js 14（App Router）+ TypeScript
- **樣式**：Tailwind CSS
- **UI 元件庫**：shadcn/ui（開發快、外觀專業，適合 3 天衝刺）
- **視覺化**：自製 SVG 電池元件 + Framer Motion（做電量變化動畫）
- **狀態管理**：React 內建 useState/useContext 即可，不需要 Redux（範圍小）
- **圖表**（一週趨勢圖）：Recharts

### 3.2 後端
- **框架**：Next.js API Routes（或 Route Handlers），前後端同一個專案，省去部署複雜度
- **語言**：TypeScript（前後端統一語言，降低 context switch 成本，3 天很重要）
- **AI 呼叫**：OpenAI API（用你拿到的 credits）
  - 模型建議：`gpt-4o-mini` 或 `gpt-4o`（依 credits 額度與速度需求選擇，mini 版夠快且便宜，適合即時互動）
- **驗證**：Zod 做 API 輸入驗證

### 3.3 資料庫
- **選擇：Cloudflare D1**（SQLite-based，原生整合 Cloudflare Pages/Workers，符合你習慣用 Cloudflare 的需求）
- **ORM**：Drizzle ORM（輕量、TypeScript 原生、對 D1 支援良好）
- **資料表設計（簡化版）**：
  - `users`（id, email/anonymous_id, personality_profile_json, base_battery_capacity, created_at）
  - `activities`（id, user_id, type, headcount, familiarity, duration_minutes, scheduled_at, predicted_drain, actual_drain, created_at）
  - `weekly_snapshots`（選配，若時間夠可做歷史趨勢）

### 3.4 部署（Cloudflare 全家桶）
- **前端 + 後端**：Cloudflare Pages（Next.js 用 `@cloudflare/next-on-pages` 轉換）
  - 或改用 **Cloudflare Workers + Hono** 若 Next.js 在 Pages 上適配遇到問題（備案）
- **資料庫**：Cloudflare D1
- **環境變數/API Key 管理**：Cloudflare Pages 的 Environment Variables（Secret 類型存 OpenAI API Key）
- **網域**：Cloudflare 提供的預設子網域即可（`xxx.pages.dev`），不需要額外買網域

### 3.5 之後要包裝成 App 的路徑（3 天後）
- 用 **Capacitor** 把 Next.js Web App 包裝成 iOS/Android App（不用重寫程式碼）
- 或用 **PWA**（Progressive Web App，加上 manifest.json + service worker）讓使用者可以「加到主畫面」，成本最低

---

## 4. UI/UX 設計概念

### 4.1 視覺風格
- **主色調**：柔和漸層（例如薄荷綠 → 淺藍，象徵「充電」與「平靜」），搭配警示用的珊瑚橘/紅色表示低電量
- **字體**：圓潤、友善的無襯線字體（例如 Inter 或思源黑體），避免銳利、冰冷的科技感——這是心理健康工具，不是金融儀表板
- **語氣**：介面文案要溫暖、非評判性（避免「你又滑手機了」的罪惡感語氣，改用「你今天的電量狀況是...」）

### 4.2 核心畫面
1. **Onboarding / 人格快篩**：一次一題，選擇題卡片式呈現，進度條在上方，結束後顯示「你的社交電池類型」摘要卡片
2. **今日電量首頁**：大型電池視覺化置中（像手機電量圖示放大版），下方顯示今日已安排/已完成的活動列表
3. **新增活動**：底部彈出式表單（bottom sheet），欄位盡量用選擇題/滑桿而非自由輸入，降低操作成本
4. **一週總覽**：橫向時間軸，每天一個迷你電池圖示，顏色代表電量高低，點擊某天可展開細節與 AI 建議
5. **活動結束回報**：活動時間到後，簡單的推播式卡片「這次社交消耗比預期... 多 / 差不多 / 少」，一鍵回報

### 4.3 互動細節
- 電池從滿到空的動畫要流暢（Framer Motion 的 spring 動畫），這是 demo 現場最容易讓評審印象深刻的視覺焦點
- 低電量時（<20%）電池顏色漸變成警示色，並跳出溫和提醒而非強制彈窗

---

## 5. 資安考量清單

### 5.1 基本防護
- [ ] 所有 API Routes 做輸入驗證（Zod schema），避免注入攻擊
- [ ] OpenAI API Key 只存在後端環境變數，絕不暴露在前端程式碼或瀏覽器 network tab
- [ ] Cloudflare Pages 環境變數設為 Secret 類型（非 Plaintext）
- [ ] 所有對外 API 呼叫走 HTTPS（Cloudflare 預設提供）

### 5.2 資料保護
- [ ] 使用者的心理測驗結果、社交活動紀錄屬於敏感個人資料，資料庫欄位不做不必要的明文備註欄位
- [ ] 若用 email 註冊，密碼需 hash（如用 bcrypt 或改用 magic link / 匿名 session 避免處理密碼）
- [ ] 匿名模式優先：demo 階段可先用瀏覽器 local session id，不強制註冊，降低個資風險與開發時間

### 5.3 API 濫用防護
- [ ] 對 OpenAI API 呼叫加上速率限制（Cloudflare Rate Limiting 或簡單的 in-memory throttle），避免 demo 現場被大量請求刷爆 credits
- [ ] 前端表單做基本 client-side rate limit（防止使用者連續狂點造成大量 API 呼叫）

### 5.4 內容安全
- [ ] AI 生成的建議文字需過濾極端/不當內容（可在 prompt 中加入安全規範，要求 AI 避免給予醫療診斷或危機介入建議，並在使用者輸入疑似心理危機關鍵字時導向專業資源，而非由 AI 自行處理）
- [ ] 免責聲明：App 內明確聲明「本工具非醫療診斷工具，如有嚴重心理困擾請尋求專業協助」

### 5.5 Cloudflare 層級
- [ ] 開啟 Cloudflare 的 Bot Fight Mode / 基本 WAF 規則
- [ ] D1 資料庫存取僅限後端 Worker/Pages Function，不對外暴露直接查詢介面

---

## 6. 3 天時程規劃

### Day 1（9/4）：地基
- 上午：確定題目、團隊分工、建立 GitHub repo、Next.js + Cloudflare Pages 專案初始化
- 下午：D1 資料庫 schema 設計與建立、社交人格快篩問卷（前端 + 邏輯）
- 晚上：電量消耗計算的 prompt 設計與測試（用 OpenAI API 跑幾組測試案例）

### Day 2（9/5）：核心功能
- 上午：新增活動表單 + API route 串接、電池視覺化元件（SVG + 動畫）
- 下午：一週總覽頁面、AI 風險預警邏輯串接
- 晚上：事後回報校準功能、UI 細節打磨（配色、字體、間距）

### Day 3（9/6 上午 11:00 前提交）
- 整合測試、修 bug、Cloudflare 正式部署與環境變數確認
- 準備 demo 腳本（用一個「假想使用者一週」情境走一遍完整流程）
- 錄製備用 demo 影片/截圖（防止現場網路或伺服器問題）
- 準備 1 分鐘 pitch：問題 → 解法 → demo → 心理學依據 → 技術亮點

---

## 7. 給 Claude Code / Codex 的開發任務清單

> 使用方式：可將以下每一項貼給 Claude Code 或 Codex，作為獨立的開發任務 prompt，或整份丟給 AI agent 依序執行。

### 任務 0：專案初始化
```
建立一個 Next.js 14 (App Router) + TypeScript 專案，整合 Tailwind CSS 與 shadcn/ui。
設定專案可部署到 Cloudflare Pages（使用 @cloudflare/next-on-pages）。
建立基本資料夾結構：/app, /components, /lib, /db。
```

### 任務 1：資料庫 Schema（Cloudflare D1 + Drizzle ORM）
```
用 Drizzle ORM 定義以下 Cloudflare D1 資料表 schema：
1. users：id (主鍵), anonymous_session_id, personality_profile (JSON), base_battery_capacity (number), created_at
2. activities：id (主鍵), user_id (外鍵), type (string), headcount (number), familiarity (1-5), duration_minutes (number), scheduled_at (datetime), predicted_drain (number, 0-100), actual_drain (number, nullable), created_at
產生 migration 檔案，並寫一個 seed script 塞入 3-5 筆測試資料。
```

### 任務 2：社交人格快篩問卷
```
建立一個 6 題單選題的 Onboarding 流程頁面（/app/onboarding），每題一張卡片、進度條在頂部。
題目要能推論出使用者的「基礎社交電池容量」（0-100 的數字）與簡短人格摘要文字。
完成後呼叫 API 將結果存入 users 表，並導向首頁。
UI 使用 shadcn/ui 的 Card, Button, Progress 元件，配色使用柔和的薄荷綠/淺藍漸層。
```

### 任務 3：電量消耗預測 API
```
建立一個 API route（/app/api/predict-drain），接收活動資訊 (type, headcount, familiarity, duration_minutes) 
與使用者的 base_battery_capacity、personality_profile，呼叫 OpenAI API（gpt-4o-mini）
根據心理學上的社交電量消耗邏輯，回傳一個 0-100 的 predicted_drain 數值與一句簡短說明文字。
Prompt 需明確要求 AI 只回傳 JSON 格式：{ "predicted_drain": number, "reason": string }。
加入 Zod 做輸入驗證與輸出驗證。
```

### 任務 4：新增活動表單 UI
```
建立一個底部彈出式表單（bottom sheet，用 shadcn/ui 的 Sheet 元件），
欄位包含：活動類型（選單：聚餐/開會/約會/上課/派對/其他）、人數（滑桿）、
熟悉度（1-5 滑桿，1=完全陌生人，5=最親密的人）、時長（滑桿，15分鐘-4小時）、日期時間（date picker）。
送出後呼叫任務 3 的 predict-drain API，並將結果存入 activities 表。
```

### 任務 5：電池視覺化元件
```
用 SVG + Framer Motion 建立一個電池視覺化元件（components/BatteryGauge.tsx），
接收 0-100 的數值 props，電量高時填色為薄荷綠，低於 20% 時漸變為珊瑚橘/紅色，
電量變化時要有平滑的液體填充動畫（spring animation）。
元件要能設定大小（今日首頁用大尺寸，一週總覽用小尺寸迷你版）。
```

### 任務 6：今日電量首頁
```
建立首頁（/app），置中顯示今日的大型 BatteryGauge，
下方列出今日已安排的活動清單（時間、類型、predicted_drain），
提供「新增活動」按鈕開啟任務 4 的表單。
```

### 任務 7：一週總覽頁面
```
建立 /app/week 頁面，橫向排列 7 天，每天顯示一個迷你 BatteryGauge（當天所有活動 drain 加總後的剩餘電量）。
呼叫 OpenAI API 分析整週資料，若某天電量低於 30%，在該天下方顯示一句 AI 生成的風險提醒與建議
（例如：「週三有兩場社交活動，建議中間安排 30 分鐘獨處恢復」）。
```

### 任務 8：事後回報校準
```
建立一個機制：當 activities 表中某筆資料的 scheduled_at 時間已過且 actual_drain 為 null 時，
在首頁顯示一張回報卡片，讓使用者選擇「比預期多/差不多/少」三個選項之一，
更新該筆 actual_drain，並呼叫 API 微調該使用者的 base_battery_capacity 或未來預測權重
（可用簡單規則：實際 vs 預期差距 > 20% 時，調整 base_battery_capacity ±5）。
```

### 任務 9：資安與部署設定
```
1. 檢查所有 API routes 是否都用 Zod 做輸入驗證。
2. 確認 OpENAI_API_KEY 只在伺服器端環境變數讀取，未出現在任何 client component 中。
3. 產生 wrangler.toml 設定檔，設定 D1 binding。
4. 撰寫 README，包含 Cloudflare Pages 部署步驟（wrangler pages deploy）與環境變數設定說明。
5. 在 OpenAI API 呼叫的 prompt 中加入安全規範：不提供醫療診斷、偵測到危機關鍵字時回傳制式的專業資源建議文字。
```

### 任務 10：Demo 資料準備
```
撰寫一個 seed script，建立一個「假想使用者一週」的完整情境資料
（包含人格測驗結果、7 天內的多筆活動、其中至少 2 天電量過低觸發風險提醒），
方便 demo 時直接展示完整流程，不需要現場即時輸入所有資料。
```

---

## 8. Demo 現場注意事項
- 準備離線截圖/影片備案，避免現場 Wi-Fi 不穩定影響 API 呼叫
- Pitch 時間通常很短，開場直接用一句話點出痛點（「你知道自己什麼時候會社交過載嗎？」），再進入 demo
- 強調心理學理論依據（Social Battery、Big Five Extraversion）與 AI 的個人化學習能力（事後校準機制），這是評審容易記住的差異化亮點

---

## 9. 心理學參考文獻（提升說服力用）

1. **Eysenck, H. J. (1967).** *The Biological Basis of Personality.* — 提出外向性的生理喚醒理論：內向者靜息狀態下的皮質喚醒程度較高，外向者較低，因此兩者對外部刺激（含社交互動）的耐受度與需求不同。這是「內向者社交電量較快耗盡」說法的理論源頭。

2. **Costa, P. T., & McCrae, R. R. (1992).** *NEO-PI-R Professional Manual.* — Big Five 人格模型將「外向性」列為五大核心特質之一，衡量個體對社交互動與外部刺激的偏好程度，是本專案「社交人格快篩」問卷設計的理論依據。

3. **Hobfoll, S. E. (1989). Conservation of Resources: A New Attempt at Conceptualizing Stress.** *American Psychologist.* — 資源保存理論指出，心理資源是有限的，人會努力保護、恢復並累積資源；資源耗損（例如社交互動中的情緒勞動）會引發壓力反應。這是「電池/資源」比喻的核心理論基礎，直接支持本專案把社交能量當作可計算、可規劃資源的產品邏輯。

4. **Baumeister, R. F., Bratslavsky, E., Muraven, M., & Tice, D. M. (1998). Ego Depletion: Is the Active Self a Limited Resource?** *Journal of Personality and Social Psychology.* — 自我調節資源會隨使用而暫時性耗竭，可解釋為何長時間或高強度社交後，情緒調節與專注力會下降，支持「事後回報校準」機制背後的行為假設。

5. 大眾心理學整理文章（如 Simply Psychology 對 social battery 概念的整理）指出，社交能量的恢復與大腦「預設模式網路（default mode network）」在安靜獨處時的活躍運作有關——此為輔助性科普說明，核心論證仍以上述 1–4 項學術文獻為主。

> 說明備註：「social battery」一詞本身並非臨床診斷用語，而是奠基於外向性人格理論與資源保存理論的實用框架，簡報中建議如此定位以維持專業嚴謹度。

## 10. 多 Agent 平行開發策略

三天時程建議採用三條並行開發軌道（詳細任務拆解見隨附的 CLAUDE.md）：

- **Track A（建議 Claude Code）**：資料庫 Schema、後端 API、資安與部署設定
- **Track B（建議 Codex）**：前端 UI 元件、頁面組裝、視覺化動畫
- **Track C（建議由本人主導）**：AI Prompt 工程、心理學邏輯把關、Demo 情境資料設計

**關鍵前提**：開工前必須先鎖定共用型別定義（`/lib/types.ts`）與 API 路由命名規則，三條軌道都以此為契約，避免最後合併時型別對不上。建議用 `git worktree` 讓每個 agent 在獨立資料夾工作，並於每天結束前做一次強制整合檢查點（見 CLAUDE.md 末尾的整合檢查點清單）。

## 11. AMD Bounty 評估結論

經評估，**暫不將 AMD Developer Cloud 部署排入 3 天開發任務**，原因：
- Bounty 名額少（僅 3 組）、獎勵非現金（AUP credits），報酬與投入時間不成比例
- 需要額外學習 AMD Developer Cloud / vLLM 部署，與現有 Cloudflare + OpenAI 技術棧無關，容易分散開發心力
- 核心 MVP 完整度與心理學論述深度才是評審重點，非贊助商技術數量

若 Day 2 晚上核心功能已完成且有餘裕，可考慮將「一週風險預警文字生成」這個非核心功能改接 AMD Developer Cloud 上的開源模型（如 Llama 3.1 8B via vLLM，相容 OpenAI API 格式），作為加分項嘗試，但預設不排入時程。
